Процедура ОбработкаПроведения(Отказ, РежимПроведения)
		
	Запись = РегистрыНакопления.СобранныеДеньги.СоздатьЗапись();
	Запись.Период = ТекущаяДата();
	Запись.Именинник = ЭтотОбъект.Именинник;
	Запись.КтоСдал = ЭтотОбъект.КтоСдал;
	Запись.СуммаСдано = ЭтотОбъект.СуммаСдано;
	Запись.Записать();
КонецПроцедуры